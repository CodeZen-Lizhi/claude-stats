import SwiftUI

enum TerminalPalette {
    static let stageBackground = Color(red: 0.034, green: 0.036, blue: 0.043)
    static let chromeBackground = Color(red: 0.055, green: 0.057, blue: 0.065)
    static let chromeRaised = Color.white.opacity(0.10)
    static let terminalBackground = Color(red: 0.030, green: 0.034, blue: 0.040)
    static let stroke = Color.white.opacity(0.13)
    static let strokeStrong = Color.white.opacity(0.28)
    static let text = Color.white.opacity(0.90)
    static let muted = Color.white.opacity(0.58)
    static let dimmed = Color.white.opacity(0.36)
    static let accent = Color.stxAccent
    static let success = Color(red: 0.44, green: 0.96, blue: 0.78)
    static let magenta = Color(red: 0.93, green: 0.26, blue: 0.71)
}
